spring:
  application:
    name: konan
  cloud:
    consul:
#      host: 192.168.1.120
      config:
        format: yaml
        watch:
          delay: 30000
      discovery:
        health-check-interval: 60s
